# TODO

## Pendientes

- [ ] Entrenar modelo de segmentación: ejecutar en máquina GPU
      `python -m ml_models.utils.train_pool_seg --data dataset.yaml --epochs 200`
      y copiar `pool_seg.onnx` a `servidor/ml_models/`

## Ideas / Mejoras futuras

- (vacío)
