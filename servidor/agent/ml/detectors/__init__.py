"""
Detectores de objetos — Cada fichero .py en este directorio define un detector.

Los detectores se registran automáticamente al decorar la clase con ``@detector``
del módulo ``agent.ml.registry``. El auto-discovery importa todos los módulos
de este paquete al consultar el registry por primera vez.
"""
